```{r}
# install.packages("mlflow")
library(mlflow)
```

```{r}
# getwd()
mlflow_set_tracking_uri("../mlruns/mlruns")
mlflow_list_experiments()$name
mlflow_search_runs(filter = "metrics.loss < 1", experiment_ids = "0")$metrics
```